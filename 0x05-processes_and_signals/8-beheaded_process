#!/usr/bin/env bash
# 8-beheaded_process

# Find the PID of the 7-highlander process
pid=$(pgrep -f "7-highlander")

# Send SIGKILL (signal 9) to the process if found
if [ -n "$pid" ]; then
    kill -9 "$pid"
fi
